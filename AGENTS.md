# Agent Notes

- Local verification before committing: run `npm run build`.
- Do not start local dev/preview servers unless the user explicitly asks for one.
- If the build passes and there are no unresolved issues, the changes can be committed and pushed to GitHub.
